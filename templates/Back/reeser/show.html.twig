{% extends 'Base.html.twig' %}

{% block title %}Ticket index{% endblock %}

{% block body %}
 
<div class="">
    <div class="d-flex justify-content-center">
        <ul class="nav">
            <li class="nav-item me-3">
                <a class="nav-link btn btn-custom-primary" href="{{ path('app_ticket_index') }}">
                    </i>
                    <span>Ticket</span>
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link btn btn-custom-primary" href="{{ path('app_reeser_index') }}">
                    </i>
                    <span>Reservation</span>
                </a>
            </li>
        </ul>
    </div>
</div>

     


  
      <main id="main" class="main">
    <table class="table">
        <tbody>
            <tr>
                <th>nombre reservation </th>
                <td>{{  reeser.nombre }}</td>
            </tr>
            <tr>
                <th>nom reservation</th>
                <td>{{reeser.nomreser }}</td>
            </tr>
            <tr>
                <th>date</th>
                <td>{{ reeser.date|date('Y-m-d') }}</td>
            </tr>
           
        </tbody>
    </table>
    <a href="{{ path('app_reeser_index') }}">back to list</a>

    <a href="{{ path('app_reeser_edit', {'id': reeser.id}) }}">edit</a>

    {{ include('Back/reeser/_delete_form.html.twig') }}
{% endblock %}
