<form method="post" action="{{ path('app_reeser_delete', {'id': reeser.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reeser.id) }}">
    <button class="btn">Delete</button>
</form>
